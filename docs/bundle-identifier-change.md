# Bundle Identifier 调整

主应用标识改为 `cc.xjpz.KidoX`。IPC 框架、Dock 插件、测试 Bundle 和卸载辅助程序使用同一前缀；辅助程序名称、Mach service、嵌入路径、双方签名要求、Dock 偏好域与通知名同步调整。日志查询和 DMG 打包路径同步更新。

应用的标准偏好域改为新标识，不自动复制旧偏好。布局数据仍保存在 `Application Support/KidoX/pages.json`。用户可通过已有设置备份恢复偏好；备份格式、扩展名和 `com.clyapps.kidox.backup` 文件类型标识保持不变，以兼容旧备份。

系统可能将新标识视为另一个应用，需要重新授权已有系统权限。签名团队与证书沿用现有配置；本次不安装或移除系统级特权辅助程序，不删除旧偏好和用户数据。

验证：独立目录签名 Release 构建通过，65 项测试通过；主 App、IPC、Dock 插件产物标识正确，主 App 与辅助程序双方签名要求匹配。安装版已替换为新标识，完整文件一致且仅有安装版进程运行；临时旧版副本已移除。界面自动化的应用列表尚未识别新标识，因此未通过它验证界面与备份导入；用户可自行导入已导出的备份。此次修改未暂存、提交或推送。
