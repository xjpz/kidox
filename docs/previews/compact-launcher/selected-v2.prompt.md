# 第 3 张修订版提示词

生成方式：内置 image_gen，编辑已有预览。

唯一图像输入：direction-3.png。目标：双图标直接切换，去掉所有固定角标，保留其他布局。

Use case: precise-object-edit, native macOS UI mockup. Edit ONLY the attached selected KidoX compact launcher preview. Preserve the entire image composition, floating panel size and placement, lavender desktop background, milky translucent material, rounded corners, shadow, every app icon and label, all six columns and four rows of 24 icons, grid spacing, search field, KidoX wordmark, and expand/ellipsis controls. Output same 1487x1058 pixel aspect and composition as input.
Make precisely two changes:
1. Replace toolbar text "常用" and its downward dropdown chevron with TWO adjacent small icon-only navigation buttons, directly selectable, in the SAME toolbar slot between KidoX and search. Left button: a five-point star for frequent apps, selected, dark gray filled star centered on a very subtle small rounded translucent white selection background. Right button: a four-square grid symbol for all apps, unselected muted gray. Both buttons equal ~34x34 logical points with about 6pt gap; icon glyphs ~17pt. No visible "常用" or "全部" text, no downward chevron, no dropdown menu, no second toolbar row, no sidebar, no heavy border. These are a star and a grid, not two stars. Keep search field "搜索应用" as is; compact the two buttons within the former selector region.
2. REMOVE ALL four pushpin badges and their white circular backing from the first four app icons (Google Chrome, 微信, ChatGPT, Xcode). Cleanly restore underlying background and icon edges. Absolutely no pushpin, star, dot, ribbon, badge, or replacement marker attached to ANY app icon. All app icon tiles must be clean, visually equal, with no special decoration for pinned status.
All other pixels/layout should remain visually faithful. No extra annotations, no new features, no captions, no diagram labels. This is a single revision of the chosen preview, not a new alternative.
